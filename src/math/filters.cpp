/**
 * @file    filters.cpp
 * @brief   Software fusion filter implementations — STUB.
 * @details Empty implementation file corresponding to filters.h.
 *          Add filter implementations here when the encoder layer is built.
 * @inputs  None (stub).
 * @outputs None (stub).
 * @deps    math/filters.h
 */

#include "math/filters.h"

// STUB — no implementations yet.
