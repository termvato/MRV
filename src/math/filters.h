/**
 * @file    filters.h
 * @brief   Software fusion filter interface — STUB.
 * @details Placeholder for future complementary or Kalman filters.
 *          The LSM6DSV16X SFLP engine handles sensor fusion in hardware,
 *          so no software filter is needed for the IMU orientation path.
 *
 *          This module is reserved for:
 *            - Joint-angle smoothing filters once encoders are added.
 *            - Any supplemental filtering needed for control input signals.
 *            - A backup software fusion path if SFLP is disabled.
 *
 * @inputs  Raw sensor or encoder values (future).
 * @outputs Filtered values (future).
 * @deps    None (stub).
 */

#pragma once

// =============================================================================
// Filters  (STUB — all empty)
// =============================================================================

// Intentionally empty.  Add filter class declarations here when required.
